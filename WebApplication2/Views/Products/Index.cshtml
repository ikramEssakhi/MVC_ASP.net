@model IEnumerable<WebApplication2.Models.Product>

@{
    ViewData["Title"] = "Index";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
<!-- Font Awesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"
      rel="stylesheet" />
<!-- Google Fonts -->
<link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap"
      rel="stylesheet" />
<!-- MDB -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.css"
      rel="stylesheet" />
<!-- MDB -->
<script type="text/javascript"
        src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.2/mdb.min.js"></script>
<style>
    .card:hover{
        transform: scale(1.1);
        transition:transform opacity 0.3s ease-in-out;
        background-image:none !important;
    }

    .card:hover .card-body{
        display:flex !important;
        justify-content: space-around;
    }
</style>

<div class="row d-flex justify-content-end">
    <div class="col-1">
        <a asp-action="Create" class="btn btn-outline-secondary">
            <i class="fa-brands fa-wpforms"></i>
        </a>
    </div>
</div>
<div class="container bg-light mt-5">
    <div class="row ">

        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">

            <div class="card" style="background-image:url(@item.imagePath); background-size:cover; background-repeat:no-repeat; background-position-x:center; background-position-y:center;height:120px">
                <div class="card-body mt-3" style="display:none">
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary col-3 d-flex align-content-center justify-content-center" style="height:35px">
                        <i class="fas fa-pen"></i>
                    </a>
                        <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-danger col-3 d-flex align-content-center justify-content-center" style="height:35px">
                        <i class="fas fa-info"></i>
                     </a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-primary col-3 d-flex align-content-center justify-content-center" style="height:35px">
                        <i class="fas fa-trash"></i>
                     </a>

                    <form class="col-3" asp-action="Create" asp-controller="CartItems">
                        <input name="Product_Id" type="text" value=@item.Id hidden />
                        <input name="Cart_Id" type="text" value="1" hidden />
                            <button type="submit" class="btn btn-danger col-3 d-flex align-content-center justify-content-center" style="height:35px">
                            <i class="fa-solid fa-cart-plus"></i>
                        </button>
                    </form>
                </div>
            </div>
            </div>
        }
    </div>

</div>

</table>

